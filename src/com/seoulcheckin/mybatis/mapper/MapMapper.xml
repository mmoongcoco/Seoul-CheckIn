<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Map">
	
	<select id="selectAll" parameterType="mapVO" resultType="mapVO">
		select map_number, map_classification, map_name, map_address, map_photo from tbl_map where map_classification = #{mapClassification} and map_approval = #{mapApproval}
	</select>
	
	<select id="selectOne" parameterType="_int" resultType="mapVO">
		select * from tbl_map where map_number = #{mapNumber}
	</select>
</mapper>